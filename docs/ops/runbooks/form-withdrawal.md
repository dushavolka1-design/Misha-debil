# Runbook: Form withdrawal

1. Set form_version status to withdrawn/unpublished; stop fill/download for new sessions.
2. Existing generated PDFs: retain per retention; show banner that template withdrawn.
3. Notify users with in-progress drafts (no legal advice — “форма снята с публикации”).
4. Archive underlay + coord map hashes for audit.
5. Update catalog tests / golden pixel baselines if replacement published.

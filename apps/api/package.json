{
  "name": "@dar/api",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "lint": "python -m ruff check app tests",
    "typecheck": "python -m mypy app",
    "test": "python -m pytest -q",
    "openapi:export": "python -m app.scripts.export_openapi"
  }
}
